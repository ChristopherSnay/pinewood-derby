# Pinewood Derby

This repository hosts a small React + Vite application used to visualize Pinewood Derby race results and standings.

Important notes
- This code was auto-generated and scaffolded with GitHub Copilot.
- The primary purpose of this repository is to build and publish the site to GitHub Pages (gh-pages branch).

Deployment
- Build: `npm run build`
- Publish to GitHub Pages: `npm run deploy` (this runs `predeploy` then `gh-pages -d dist`).
- A `.nojekyll` file is included in `public/` so the built `dist/` directory is served correctly by GitHub Pages.

Hosting URL
- https://christophersnay.github.io/pinewood-derby

Notes and next steps
- If assets are 404ing, add a `homepage` field to `package.json` set to your Pages URL and re-run `npm run deploy`.
- The project uses `gh-pages` (devDependency) and the `deploy` script publishes the `dist` directory to the `gh-pages` branch.

Contact
- If you want me to add the `homepage` field, commit the change, or update the README with the final URL, tell me the exact Pages URL and I'll apply it and redeploy.

